// packAlternativo.js
class PackAlternativo {
    constructor(nombre, items) {
      this.nombre = nombre;
      this.items = items;
    }
  }
  
  // Exportamos la clase
  export default PackAlternativo;
  